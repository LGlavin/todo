- if tasks.empty?
  %span.text-warning There are no tasks!
- else
  %table.table.table-hover.table-bordered
    %thead
      %tr
        %th Title
        %th Created at
        %th Completed
        %th Actions
      %body
        - tasks.each do |task|
          %tr
            %td
              %strong= task.title
            %td.text-info= task.created_at
            %td.text-success= task.completed
            %td
              = link_to task_path(task), remote: true, method: :delete, data: {confirm: "Do you really want to delete #{task.title}?"} do
                %i.icon-remove
              = link_to edit_task_path(task), remote: true do 
                %i.icon-edit 